# Docs Assets

These images are documentation-only assets.

- `concept-editorial-minimal.png` is a directional visual reference for the Editorial Minimal style. It is not an implementation scope commitment; graph, templates, and plugin-like features remain out of scope for the MVP.
- `app-logo-source.png` is the source concept image for the app icon.

Do not commit screenshots from real personal vaults or images that expose private note titles, folder names, or note content.
